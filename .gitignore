# Local .terraform directories
**/.terraform/*
terraform\.d/*
.terraform
.terraform.lock.hcl

# No state files in VCS. They should be stored in a remote location.
*.tfstate
*.tfstate.*
tfplan
.tool-versions

# Temporary files
**/terraform.plan*
*debug*.log
*_v2*
*.log
*.zip

# Junk
.DS_Store

# Any locally saved secrets
**/.env.local
shared/aws-ecs-cp-swap
*.hcl
